#include <stdio.h>
#include "main.h"
/**
 * _isupper - checks if the char is uppercase
 * Return: 1 if upper 0 if not
 * @c: char to be checked
 */

int _isupper(int c)
{
	if
	(c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
