#include "main.h"
#include <stdio.h>
/**
 * _isdigit - checks if a char is a dig
 * Return: 1 if c is a dig 0 otherwise
 * @c: char to be checked
 */

int _isdigit(int c)
{
	if
	(c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
